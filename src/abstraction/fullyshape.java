// Fully abstract class
package abstraction;
abstract class Shape {
    // Abstract method for calculating the area (no body)
    public abstract double calculateArea();

    // Abstract method for calculating the perimeter (no body)
    public abstract double calculatePerimeter();
}
